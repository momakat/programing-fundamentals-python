def all_characters(first_character,second_character):
    characters = []
    for character in range(ord(first_character)+1,ord(second_character)):
        characters.append(chr(character))
    return characters


first_char = input()
second_char = input()

result = all_characters(first_char,second_char)

print(" ".join(result))
